#include "holberton.h"
/**
 * _abs - checks the absolute value
 *
 *@int: intiger value
 *
 * Return: returns the value of _abs
 */

int _abs(int)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
}
