#include <stdio.h>
/**
 *  *  * main - Entry point
 *   *   *
 *    *    * Return: Always 0 (Success)
 *     *     **/
int main(void)
{
char all;
	for (all = 'a'; all <= 'z'; all++)
	{
		putchar(all);
	}
	return (0);
}
