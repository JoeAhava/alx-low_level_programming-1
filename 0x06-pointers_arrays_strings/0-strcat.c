#include "holberton.h"

/**
 * _strcat - main function
 *
 * @dest: char pointer array
 *
 * @src: cgar pointer array
 *
 * Return: char dest
 */

char *_strcat(char *dest, char *src)
{
	int i;
	int j = 0;

	i = 0;
	while (dest[i] != 0)
		a++;
	j = 0;
	while (src[j] != 0)
	{
		dest[i] = src[j];
		i++;
		j++;
	}
	return (dest);
}
