#include "holberton.h"

/**
 * _strcat - main function
 *
 * @dest: char pointer array
 *
 * @src: cgar pointer array
 *
 * Return: char dest
 */

char *_strcat(char *dest, char *src)
{
	int i = 0;

	for (i = 0; i < n; i++)
		*(s + i) = b;
	return (dest);
}
